[package]
name = "spec-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
peam-ssz = { path = ".." }
snap = "1"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
hex = "0.4"
